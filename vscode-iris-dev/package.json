{
  "name": "vscode-iris-dev",
  "displayName": "iris-dev for IRIS",
  "description": "Wires iris-dev MCP tools into VS Code Copilot agent mode — automatically picks up your objectscript.conn connection. Requires the iris-dev binary on PATH.",
  "version": "0.4.22",
  "publisher": "intersystems-community",
  "license": "MIT",
  "engines": {
    "vscode": "^1.99.0"
  },
  "categories": [
    "AI",
    "Other"
  ],
  "keywords": [
    "intersystems",
    "iris",
    "objectscript",
    "mcp",
    "copilot"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onMcpCollection:iris-dev"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "mcpServerDefinitionProviders": [
      {
        "id": "iris-dev",
        "label": "iris-dev (IRIS)"
      }
    ],
    "configuration": {
      "title": "iris-dev",
      "properties": {
        "iris-dev.containerName": {
          "type": "string",
          "default": "",
          "description": "Name of the Docker container running the IRIS instance. Used to provide the IRIS_CONTAINER environment variable to the iris-dev server process, which allows it to use tools which require direct access."
        },
        "iris-dev.serverPath": {
          "type": "string",
          "default": "",
          "description": "Full path to the iris-dev binary. Leave empty to auto-discover via PATH. Use this if VS Code cannot find the binary."
        },
        "iris-dev.tlsVerify": {
          "type": "boolean",
          "default": true,
          "description": "Verify TLS certificates when connecting to IRIS over HTTPS. Set to false for self-signed certificates. Also disabled automatically when http.proxyStrictSSL is false."
        },
        "iris-dev.namespace": {
          "type": "string",
          "default": "",
          "description": "IRIS namespace override. Leave empty to use the namespace from objectscript.conn. Useful when you want iris-dev to target a different namespace than your editor connection."
        },
        "iris-dev.scheme": {
          "type": "string",
          "default": "",
          "enum": ["", "http", "https"],
          "enumDescriptions": [
            "Auto-detect from objectscript.conn / intersystems.servers (default).",
            "Force HTTP.",
            "Force HTTPS. Use with iris-dev.tlsVerify: false for self-signed certificates."
          ],
          "description": "HTTP scheme override for the IRIS web server connection. Leave empty to auto-detect."
        },
        "iris-dev.toolset": {
          "type": "string",
          "default": "baseline",
          "enum": ["baseline", "nostub", "merged"],
          "enumDescriptions": [
            "Standard tool set for ObjectScript development.",
            "Excludes stub/preview tools. Use when you want a stable, production-ready tool set.",
            "Extended tool set including interop production management and container lifecycle tools."
          ],
          "description": "Controls which iris-dev tools are registered with the MCP server."
        }
      }
    }
  },
  "scripts": {
    "compile": "esbuild src/extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node",
    "package": "npm run compile && vsce package --no-dependencies"
  },
  "dependencies": {
    "which": "^6.0.1"
  },
  "devDependencies": {
    "@intersystems-community/intersystems-servermanager": "^3.8.0",
    "@types/node": "^20",
    "@types/vscode": "^1.99.0",
    "@types/which": "^3.0.4",
    "@vscode/vsce": "^3",
    "esbuild": "^0.28.0",
    "typescript": "^5"
  }
}
